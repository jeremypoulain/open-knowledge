---
'@inkeep/open-knowledge': patch
---

Improves `All files` search so text/code hits open the matched file at the first matching line instead of only opening the file root. PDF hits still deep-link to the matched page.
