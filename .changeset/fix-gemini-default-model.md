'@inkeep/open-knowledge': patch
---

Fix AI provider settings so `gemini` starts with Google's OpenAI-compatible base URL and a valid Gemini chat model, and custom provider `model` / `baseUrl` edits in Settings persist reliably after you save a key.
