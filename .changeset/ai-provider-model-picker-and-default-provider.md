'@inkeep/open-knowledge': patch
---

Improve Settings → AI so each provider can load its available model list after you add a valid key, save the selected `model` for later use, and keep the chosen default provider selected instead of snapping back to blank.
