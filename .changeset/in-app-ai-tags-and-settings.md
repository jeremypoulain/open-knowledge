---
'@inkeep/open-knowledge': patch
---

Add in-app `AI` settings for configuring provider keys, models, and base URLs without storing secrets in synced config.

Add the in-editor `AI` bubble menu for transforming selected text with a streamed accept/reject preview, and add `Suggest tags` in the toolbar plus the `⌥⌘ T` / `Ctrl Alt T` shortcut to merge AI-generated tags into frontmatter.
